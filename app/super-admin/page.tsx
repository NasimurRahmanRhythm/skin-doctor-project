/**
 * Placeholder. Phase 2 replaces this with the OTP login and the
 * role-based redirect into each desk (reception / nurse / doctor / owner).
 */
export default function SuperAdminPage() {
  return (
    <main className="flex flex-1 items-center justify-center px-6 py-24">
      <div className="w-full max-w-md border border-line bg-paper px-8 py-10 text-center rounded-card">
        <h1 className="font-serif text-2xl">Staff sign in</h1>
        <p className="mt-3 text-sm text-ink-soft">
          Coming in Phase 2 — email one-time code.
        </p>
      </div>
    </main>
  );
}
